# Randomizer
The program is actually quite simple. You have a source folder containing many files (images or other types of files) and want to move a certain number of them—selected at random—to the destination folder or have them zipped directly.
